# Required only for the server-side draft endpoint. Never expose this in a browser.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-terra
# Disposable shared code required only for the live GPT-5.6 demo endpoint.
# Use at least 12 random characters and store it only in server-side runtime secrets.
QUOTEPROOF_DEMO_KEY=
